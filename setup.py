from setuptools import setup

setup(name='linkedinscraper',
      version='0.1',
      author='yujhenchen',
      packages=['pages', 'scraper'],
      install_requires=['wheel','webdriver-manager', 'selenium'])
